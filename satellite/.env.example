############################################
# Satellite Docker Compose configuration
# Copy to .env and fill in values
############################################

# Required: token issued by the platform
SATELLITE_TOKEN=replace-with-your-token

# Optional overrides
PLATFORM_URL=https://dev-api.dataforce.studio
# Public base URL of this machine (used to report inference endpoints)
# For local dev you can leave it as localhost. If the platform must
# reach you from outside your network, set this to your public URL.
BASE_URL=http://localhost

# Image the agent will launch for deployments
MODEL_IMAGE=df-random-svc:latest

# Polling interval in seconds
POLL_INTERVAL_SEC=2

